# 用户手册

## 目录

- [简介](#简介)
- [安装说明](#安装说明)
- [界面介绍](#界面介绍)
- [功能说明](#功能说明)
- [操作步骤](#操作步骤)
- [快捷键](#快捷键)
- [常见问题](#常见问题)
- [术语表](#术语表)

---

## 简介

CRF Viewer 是一款专业的差分图片无损压缩与查看工具。它支持将多张差分图（残差帧）序列压缩为紧凑的 `.crf` 格式文件，并保证完全无损还原。

### 主要功能

- **差分图压缩**：将 2~50 张差分图无损压缩为单个 .crf 文件
- **差分图查看**：逐帧浏览、缩放、平移查看差分图序列
- **序列播放**：自动播放差分图序列，支持调整播放速度
- **元数据查看**：查看文件头信息、编码参数、帧统计等
- **导出还原**：将 .crf 文件解压还原为原始差分图

### 适用场景

- 科研实验中的图像差分分析
- 图像处理算法的中间结果存档
- 嵌入式设备的图像数据传输
- 需要精确保存差分数据的任何场景

---

## 安装说明

### 下载安装

从 Releases 页面下载对应平台的安装包：

**Windows**：
- 下载 `.msi` 安装包，双击运行安装向导
- 或下载 `.exe` 免安装版本，直接运行

**macOS**：
- 下载 `.dmg` 文件，打开并将应用拖入 Applications 文件夹
- 首次运行需在「系统偏好设置 > 安全性」中允许运行

**Linux**：
- 下载 `.deb` 包：`sudo dpkg -i crf-viewer_*.deb`
- 或下载 `.AppImage`：`chmod +x crf-viewer_*.AppImage && ./crf-viewer_*.AppImage`

### 系统要求

| 项目 | 最低要求 |
| :--- | :--- |
| 操作系统 | Windows 10+ / macOS 12+ / Ubuntu 20.04+ |
| 内存 | 4GB RAM |
| 磁盘空间 | 100MB |
| 显示器 | 1280x720 分辨率 |

---

## 界面介绍

### 主界面布局

```
+================================================================+
|  菜单栏  文件  编辑  视图  帮助                                |
+================================================================+
|  工具栏  [打开] [保存] [压缩] [解压] [设置]                   |
+----------------------------------------------------------------+
|            |                              |                    |
|   序列     |                              |    元数据          |
|   面板     |        图像查看区             |    面板            |
|            |                              |                    |
|  [帧 1]   |     +------------------+     |  文件信息          |
|  [帧 2]   |     |                  |     |  编码参数          |
|  [帧 3]   |     |    当前帧图像    |     |  帧统计            |
|  ...       |     |                  |     |                    |
|  [帧 N]   |     +------------------+     |                    |
|            |                              |                    |
+----------------------------------------------------------------+
|  状态栏  帧: 1/10  |  尺寸: 1920x1080  |  位深: 8bit         |
+================================================================+
```

### 界面区域说明

**菜单栏**：包含文件操作、编辑、视图切换、帮助等菜单项。

**工具栏**：常用操作的快捷按钮，包括打开、保存、压缩、解压等。

**序列面板**（左侧）：显示当前加载的差分图序列，点击可切换帧。

**图像查看区**（中央）：显示当前选中帧的图像，支持缩放、平移。

**元数据面板**（右侧）：显示文件信息、编码参数、帧统计等元数据。

**状态栏**（底部）：显示当前帧索引、图像尺寸、位深等状态信息。

---

## 功能说明

### 1. 打开文件

支持打开以下类型的文件：

| 文件类型 | 说明 |
| :--- | :--- |
| 图像文件 | PNG、BMP、TIFF 格式的单张或多张图像 |
| CRF 文件 | 已压缩的 .crf 格式文件 |

### 2. 导入差分图序列

将多张差分图导入应用进行查看或压缩：

- 支持拖拽导入
- 支持通过菜单或工具栏按钮导入
- 自动验证图像尺寸和格式一致性
- 支持 2~50 张图像

### 3. 压缩为 CRF

将差分图序列压缩为 .crf 格式：

- 选择压缩算法（Golomb-Rice / 指数哥伦布 / 变换编码）
- 设置压缩参数
- 显示压缩进度
- 保存生成的 .crf 文件

### 4. 查看 CRF 文件

打开并查看 .crf 文件的内容：

- 显示文件元数据（版本、帧数、尺寸等）
- 逐帧浏览解码后的差分图
- 查看每帧的编码信息

### 5. 解压还原

将 .crf 文件解压还原为原始差分图：

- 选择输出目录
- 选择输出格式（PNG / BMP / TIFF）
- 显示解压进度
- 生成与原始完全一致的图像文件

---

## 操作步骤

### 步骤 1：启动应用

双击应用图标启动 CRF Viewer。

### 步骤 2：导入差分图

**方式一：拖拽导入**

1. 在文件管理器中选择要导入的差分图文件
2. 将文件拖拽到应用窗口的图像查看区
3. 应用自动验证并加载图像

**方式二：菜单导入**

1. 点击菜单栏「文件」>「打开」
2. 在文件选择对话框中选择图像文件
3. 点击「打开」确认导入

**方式三：快捷键**

1. 按 `Ctrl/Cmd + O` 打开文件选择对话框
2. 选择要导入的图像文件
3. 点击「打开」确认

### 步骤 3：预览差分图

导入后，差分图序列显示在左侧面板中：

- 点击序列面板中的帧缩略图切换查看
- 使用 `←` / `→` 方向键逐帧切换
- 使用鼠标滚轮缩放图像
- 按住鼠标左键拖动平移图像

### 步骤 4：压缩为 CRF 文件

1. 确认差分图序列无误后，点击工具栏「压缩」按钮
2. 在压缩设置对话框中：
   - 选择压缩算法类型
   - 根据需要调整参数
   - 输入用户备注（可选）
3. 点击「开始压缩」
4. 等待压缩完成（通常不到 1 秒）
5. 选择保存位置，保存 .crf 文件

### 步骤 5：查看 CRF 文件

1. 点击菜单栏「文件」>「打开」
2. 选择 .crf 文件
3. 应用自动解码并显示内容
4. 在元数据面板查看文件信息
5. 使用序列面板逐帧浏览

### 步骤 6：解压还原

1. 打开 .crf 文件后，点击工具栏「解压」按钮
2. 选择输出目录
3. 选择输出图像格式
4. 点击「开始解压」
5. 等待解压完成
6. 在输出目录中找到还原的差分图

---

## 快捷键

### 全局快捷键

| 快捷键 | 功能 |
| :--- | :--- |
| `Ctrl/Cmd + O` | 打开文件 |
| `Ctrl/Cmd + S` | 保存/导出 |
| `Ctrl/Cmd + E` | 压缩为 CRF |
| `Ctrl/Cmd + Shift + E` | 解压 CRF 文件 |
| `Ctrl/Cmd + I` | 查看文件信息 |
| `Ctrl/Cmd + ,` | 打开设置 |

### 图像浏览快捷键

| 快捷键 | 功能 |
| :--- | :--- |
| `←` | 上一帧 |
| `→` | 下一帧 |
| `Home` | 跳到第一帧 |
| `End` | 跳到最后一帧 |
| `Space` | 播放/暂停序列 |
| `+` / `=` | 放大 |
| `-` | 缩小 |
| `0` | 重置缩放（100%） |
| `F` | 适应窗口 |
| `Ctrl/Cmd + 0` | 实际大小（1:1） |

### 鼠标操作

| 操作 | 功能 |
| :--- | :--- |
| 滚轮 | 缩放图像 |
| 左键拖动 | 平移图像 |
| 双击 | 适应窗口 |
| 右键点击 | 打开上下文菜单 |

---

## 常见问题

### Q: 支持哪些图像格式？

A: 输入支持 PNG、BMP、TIFF 等常见格式。输出为自定义 `.crf` 格式。

### Q: 最大支持多少帧？

A: 50 帧是当前批量导入/编码路径的限制，不是 CRF 文件硬上限。超过 50 帧可使用流式
编码 API；当前码流字段最高可表示 65535 帧。

### Q: 压缩率如何？

A: 由于采用完全无损压缩且不做帧间预测，压缩率通常为原始大小的 50%~80%。具体取决于差分图的内容复杂度。

### Q: 打开 CRF 文件时提示"不是有效的 CRF 文件"？

A: 可能的原因：
- 文件已损坏
- 文件不是 CRF 格式
- 文件版本不兼容

请确认文件来源和完整性。

### Q: 压缩/解压速度很慢？

A: 正常情况下压缩/解压速度很快（< 100ms/帧）。如果很慢，请检查：
- 文件尺寸是否过大（建议不超过 4K 分辨率）
- 位深是否过高（16 位会比 8 位慢）
- 系统资源是否充足

### Q: 可以在 CRF 文件中添加备注吗？

A: 可以。在压缩时可以输入用户备注，最多 40 字节（UTF-8）。备注会保存在文件头中。

### Q: 如何查看 CRF 文件的详细信息？

A: 打开 CRF 文件后，右侧面板会显示完整的元数据，包括版本、帧数、尺寸、位深、压缩类型等。

### Q: 不同位深的图像可以一起压缩吗？

A: 不可以。同一序列中的所有图像必须具有相同的宽高、位深和色彩格式。

---

## 术语表

| 术语 | 英文 | 说明 |
| :--- | :--- | :--- |
| 差分图 | Difference Image | 两帧图像之间的差异图像，也称为残差帧 |
| 残差帧 | Residual Frame | 与差分图含义相同，指像素级的差值数据 |
| 无损压缩 | Lossless Compression | 压缩后可完全还原原始数据，不丢失任何信息 |
| 有符号整数 | Signed Integer | 可以表示正数、负数和零的整数类型 |
| 位深 | Bit Depth | 每个像素通道使用的二进制位数 |
| 色彩格式 | Color Format | 图像的颜色编码方式（如 RGB、YUV） |
| 子采样 | Subsampling | 降低色度分辨率以减少数据量的技术 |
| 熵编码 | Entropy Encoding | 利用数据统计特性进行压缩的编码方式 |
| Golomb-Rice | — | 一种适合小数值的熵编码算法 |
| 指数哥伦布 | Exponential-Golomb | 一种适合大动态范围的熵编码算法 |
| 变换编码 | Transform Coding | 通过数学变换集中能量后再编码的方式 |
| 魔数 | Magic Number | 文件开头用于标识格式的固定字节序列 |
| CRC32 | — | 32位循环冗余校验，用于检测数据完整性 |
| Zigzag 扫描 | Zigzag Scan | 将二维矩阵转换为一维序列的扫描方式 |
| 帧索引 | Frame Index | 记录每帧在文件中位置的索引表 |
| 元数据 | Metadata | 描述文件属性的数据（如尺寸、格式等） |
