[package]
name = "crf-cuda"
# 与 crf-viewer 版本同步(三位 semver:0.4.0.0 → 0.4.0);规则见 docs/project-standards.md §13。
version = "0.4.0"
edition = "2021"
description = "Sidecar NVIDIA CUDA driver-API backend for CRF"

[lib]
crate-type = ["cdylib"]
