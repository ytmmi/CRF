//! 码流格式常量 —— 已迁移到 `core::bitstream::constants`
//!
//! **迁移说明（P4）**：本文件内容已迁移到 [`crate::crf::core::bitstream::constants`]。
//! 保留为 `pub use` 转发层，维持 `format::constants::*` 旧路径兼容。
//! 新代码请直接使用 `core::bitstream::constants`。

pub use crate::crf::core::bitstream::constants::*;