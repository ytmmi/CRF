//! 可逆色彩空间变换 —— 已迁移到 `core::color::rct`
//!
//! **迁移说明（P4）**：本文件内容已迁移到 [`crate::crf::core::color::rct`]。
//! 保留为 `pub use` 转发层，维持 `format::rct::*` 旧路径兼容。
//! 新代码请直接使用 `core::color::rct`。

pub use crate::crf::core::color::rct::*;