# CRF 格式标准

**版本**：v1.13
**状态**：稳定
**最后更新**：2026-08-24

> 后续格式实现、模块拆分、测试和构建流程必须遵守
> [《CRF 项目开发、算法与构建标准》](docs/project-standards.md)。本文的已发布字段与
> 解码语义仍具有格式约束力；历史概述不得覆盖项目标准中已确认的有损/无损并行口径。

---

## 1. 格式概述

**.crf（Compressing Residual Frames）** 是一种轻量级的**完全无损**图像序列压缩格式，专为**二次元插画差分场景**特化设计：同一张插画衍生出多张变化版本（服装差分、表情差分、姿势差分等），帧间绝大部分像素保持不变，仅局部区域发生修改。

### 1.1 场景特征与设计契合

| 二次元插画差分的数据特征 | CRF 对应的特化机制 |
| :--- | :--- |
| 帧间大面积像素完全相同（实测零值占比 ~70%） | RLE 零行程 + 指数哥伦布行程编码，长行程高度紧凑 |
| 赛璐璐上色：大片平坦色块 + 锐利边缘 | MED / PAETH 边缘检测预测，平坦区残差恒为零 |
| 色彩数有限，色度通道常整块恒定 | YCoCg-R 可逆去相关 + 三平面独立编码（frame_type=3） |
| 变化区域局部分布，内容随帧而异 | 逐帧自适应预测模式选择 + 32 行条带级模式切换 |
| 插画本体为自然级复杂度 | PAETH/MED 竞争覆盖渐变、线稿、噪点各形态 |

### 1.2 基本属性

| 属性 | 说明 |
| :--- | :--- |
| **文件扩展名** | `.crf` |
| **MIME 类型** | `video/crf` |
| **压缩性质** | **完全无损**（所有变换数学严格可逆） |
| **目标数据** | 二次元插画差分序列，数量 2～50 张 |
| **色彩格式** | 灰度 / RGB / YUV444 / YUV422 / YUV420 |
| **位深** | 8 / 10 / 12 / 16 bit |
| **自包含性** | ✅ 单文件即可还原全部原始图像 |

### 1.3 与通用视频编码的定位差异

CRF **不做运动估计/运动补偿**——差分图的生成端（画师工作流）天然保证了逐像素对齐。省下的复杂度全部投入到**空间预测自适应、色度去相关、行程熵编码**三个无损收益最高的环节。

---

## 2. 设计目标

1. **完全无损**：解码输出与编码输入逐像素、逐比特一致（全部测试往返校验通过）。
2. **压缩率优先可调**：固定单模式路径 < 100ms/帧；自适应路径（质量优先）约 600ms/帧。
3. **自描述**：每帧帧头记录该帧实际的预测模式与熵编码类型，解码不依赖外部约定。
4. **单调不劣化**：所有"竞争式"优化（双路熵编码、条带化、三平面）仅在结果更小时采用，任何输入下不劣于基线路径。

---

## 3. 完整格式规范

### 3.1 文件结构概览

```
+---------------------+
| 文件头 (Header, 64B) |
+---------------------+
| 帧索引 (Index)       |  <- frame_count × 8 字节
+---------------------+
| 帧 0 (首帧)          |  <- 帧头(11B) + 编码数据
+---------------------+
| 帧 1 .. N-1 (差分帧) |
+---------------------+
| 文件尾 (Footer, 8B)  |  <- CRC32 + 魔数
+---------------------+
```

### 3.2 文件头（Header，64 字节，小端序）

| 偏移 | 长度 | 字段 | 说明 |
| :-- | :-- | :-- | :-- |
| 0 | 4 | `magic` | 固定 `"CRF\x00"`（0x43 0x52 0x46 0x00） |
| 4 | 2 | `version_major` | 主版本号，当前 **1** |
| 6 | 2 | `version_minor` | 次版本号，当前 **2** |
| 8 | 2 | `frame_count` | 帧总数，范围 2～50 |
| 10 | 2 | `width` | 图像宽度 |
| 12 | 2 | `height` | 图像高度 |
| 14 | 1 | `bit_depth` | 8 / 10 / 12 / 16 |
| 15 | 1 | `color_format` | 0=灰度, 1=YUV444, 2=YUV422, 3=YUV420, 4=RGB |
| 16 | 1 | `compression_type` | 0=Golomb-Rice, 1=指数哥伦布, 2=变换+熵编码 |
| 17 | 1 | `flags` | 见 3.3 |
| 18 | 2 | `block_size` | 变换块大小（compression_type=2 时有效），默认 8 |
| 20 | 1 | `prediction_mode` | 全局默认预测模式（帧级未指定时的回退值），见 3.7 |
| 21 | 3 | `reserved` | 保留，全零填充 |
| 24 | 40 | `user_data` | 用户自定义元数据（UTF-8，NULL 结尾） |

### 3.3 标志位（flags）

| 位 | 名称 | 说明 |
| :-- | :-- | :-- |
| bit 0 | `has_index` | 文件含帧索引表 |
| bit 1 | `has_rct` | 帧数据经过 YCoCg-R 可逆色彩变换（仅 3 分量格式）；bit3=1 时首帧例外 |
| bit 2 | `has_lossy_quant` | 真有损编码，量化步长见文件头 byte21 |
| bit 3 | `first_frame_no_rct` | **首帧绕过 YCoCg-R 存储**（v1.13）：编码端对首帧做「RCT 域 vs RGB 直通」双路完整管线竞争，直通胜出时置位；解码端出口须跳过 frame0 的 rct_inverse。差分帧不受影响（差分基准是首帧像素值而非存储格式）。v1.13 前恒为 0，旧文件天然兼容 |
| bit 4~7 | 保留 | 必须为 0 |

### 3.4 帧索引（Index）

当 `has_index=1` 时紧跟文件头，长度 `frame_count × 8` 字节，每项：

| 长度 | 字段 | 说明 |
| :-- | :-- | :-- |
| 4 | `offset` | 该帧起始偏移（自文件头起算） |
| 4 | `size` | 该帧总大小（含帧头） |

### 3.5 帧头（Frame Header，11 字节）

| 偏移 | 长度 | 字段 | 说明 |
| :-- | :-- | :-- | :-- |
| 0 | 4 | `frame_size` | 本帧载荷字节数（不含本帧头） |
| 4 | 4 | `pixel_count` | 本帧数据总长度（像素数 × 分量数） |
| 8 | 1 | `frame_type` | 熵编码/打包类型，见 3.6 |
| 9 | 1 | `coding_params` | 语义随 frame_type：k 值 / 条带高度 / 保留(0) |
| 10 | 1 | `pred_mode` | 本帧预测模式；`0xFF`=未指定（回退文件头全局设置） |

### 3.6 帧类型（frame_type）

| 值 | 名称 | 说明 | coding_params 语义 |
| :-- | :-- | :-- | :-- |
| 0 | 块级自适应 k Golomb | 每 8×8 块独立 k 的纯 Golomb-Rice，适合高熵数据 | 0xFF 标记 |
| 1 | RLE+Golomb | 零行程 exp-Golomb + 非零 Golomb-Rice(zigzag)，全帧单一 k 与单一预测模式 | 全帧 k 值 |
| 2 | 条带自适应 RLE+Golomb | 按条带切分（高度自 v1.9 起可变：32/64 行竞争择优），每条带独立预测模式 + 独立 k | 条带高度 |
| 3 | 三平面打包 | 拆 Y/Co/Cg 三平面（含 CfL 色度预测），各自独立走完整自适应管线后打包 | 保留(0) |
| 4 | 调色板模式 | 低色数数据（唯一值 ≤256）：palette 表 + RLE 索引流 | 保留(0)；bit0=copy-above v2 |
| 5 | RLE+CABAC 算术编码 | 与 frame_type=1 位流语法同构，escape/前缀位改由自适应算术编码承载；v3 三模式上下文分类器竞争（MA 树 / 固定梯度 / 全帧统一） | 全帧 k 值 |
| 6 | DCT 变换域量化 | 分块 lifting DCT（形状 {4×4, 8×8, 8×4, 4×8}，v1.12 矩形泛化）→ 死区量化（flat/感知矩阵/Trellis）→ CABAC（有损路径专用，无空间预测） | 全帧 k 值；载荷首字节 bit4=Trellis、bit5=宽8、bit6=矩阵、bit7=高8 |
| 7 | 帧内块复制（IntraBC，v1.11） | 8×8 块级 COPY/PRED 决策 + 三段式载荷（决策 RLE / COPY 向量 exp-Golomb / PRED 预测残差 RLE）；无损路径全帧参与（v1.11 差分帧启用） | 保留(0) |
| 8 | 预测后变换 + CABAC 系数编码（v1.14） | 8×8 块 {DC/H/V/MED} SAD 选模式 → DC 走 transform skip / 其余走 lifting DCT → 死区量化 → zigzag → CoeffCABAC（run-level，4 上下文）；三平面独立编码。**载荷自包含亮度/色度步长**（v1.14.1 起：flags bit1/bit2 后各 1 字节，见下方布局） | 保留(0)；bit7=golden 参考 |

**编码器按"字节最小者胜出"在候选间自动竞争**，逐帧记录实际类型，解码端按帧分流：

- 首帧额外参与 `0 vs 1` 双路熵编码竞争（自然插画经 MED/PAETH 预测后 RLE 常显著更优）；
- 自适应模式下残差帧参与 `帧级 top-2 / 条带(2) / 三平面(3) / 调色板(4) / CABAC(5)` 多路竞争；
- 三平面与调色板的子载荷内部可递归选用其余全部类型。

#### frame_type=2 载荷布局

```
[band_count u16 LE]
重复 band_count 次：
  [pred_mode u8][k u8][data_len u32 LE][RLE+Golomb 数据 data_len 字节]
```

条带高度由帧头 coding_params 给出：v1.8 及以前恒为 32；v1.9 起编码器在
{32, 64} 中按字节竞争择优（仅当帧高 ≥128 时启用 64 行档），解码端校验
合法值集合 {32, 64}。条带按光栅顺序排列，预测只依赖已重建上文。

#### frame_type=3 载荷布局

```
[cfl_flags u8]            高 4 位 = Co 平面 α+8；低 4 位 = Cg 平面 α+8（α∈[-4,4]，0=关闭）
重复 3 次（Y 平面、Co 平面、Cg 平面）：
  [sub_frame_len u32 LE]
  [完整子帧（自身 11B 帧头 + 载荷，色彩格式视作灰度）]
```

子帧内部可再自由选用 frame_type 0/1/2/4/5，实现逐平面最优编码。

**CfL（Chroma-from-Luma）**：Co/Cg 平面在编码前减去亮度线性预测
`⌊α·(Y−128)/16⌋`，解码端用已重建的 Y 平面对称加回（Y 先于色度平面处理，
因果安全，无损）。α 由编码器按抽样 SAD 搜索确定。

#### frame_type=4 载荷布局（调色板模式）

```
[palette_count u16 LE][k_index u8][pal_len u32 LE]
[palette 值位流 pal_len 字节（exp-Golomb zigzag）]
[索引位流（剩余字节，RLE+Golomb 自适应 k）]
```

适用于唯一值 ≤256 的低色数数据（二次元插画纯色块）；超限时编码器自动放弃该候选。

#### frame_type=5 载荷布局（RLE+CABAC）

```
[k u8][自适应算术编码位流]
```

位流语法与 frame_type=1 同构（escape/行程/非零值），区别在于：
- escape 位、非零值 Golomb 商前缀位由**自适应二值算术编码器**
  （LZMA 式 range coder，12-bit 概率精度，更新率 1/32，9 个独立上下文）承载；
- 行程长度采用"分桶直通"二值化：m<15 时标志 0 + 4 位直写 m，
  否则标志 1 + 5 位直写 m−15——规避概率模型下限对超偏斜序列的拖累；
- Golomb 余数与 exp 数据位为等概率直通（bypass）。

#### frame_type=6 载荷布局（DCT 变换域量化，v1.7+）

```
[k|标志位 u8][flags u8][(ma_tree 头)][自适应算术编码位流]
```
首字节标志位布局（低 3 位为 Golomb k 值）：

| 位 | 名称 | 说明 |
| :-- | :-- | :-- |
| bit4 | Trellis（v1.10） | 量化级经率失真 DP 择优；输出仍为自描述倍数，解码端透明 |
| bit5 | 8×8 块（v1.10） | 变换块大小标记：0=4×4，1=8×8，解码端对称选择逆变换几何 |
| bit6 | 感知矩阵（v1.9） | 量化步长按 4×4/8×8 频率位置感知权重表加粗（DC ×1.0 → 高频 ≤×2.25） |

- 三类标志仅作用于**编码端**步长分配与决策——量化残差本身即各位置
  Q_pos 倍数的自描述反量化结果，解码端 mask 标志位取 k 后行为一致；
- DCT 系数流为非空间域数据，不启用空间域上下文分类器
  （flags 恒为 Uniform）。

#### frame_type=8 载荷布局（预测后变换 + CABAC 系数编码，v1.14+）

```
[flags u8]              bit0=has_modes(预留 0)；bit1=luma_step_present；bit2=chroma_step_present
[luma_step u8]          亮度步长（无条件信令；0=无损）
[chroma_step u8]        色度步长（无条件信令）
重复 3 次（Y、Co、Cg 平面）：
  [sub_len u32 LE]
  [子载荷：mode_len u32 LE][k u8][mode_body(CABAC 模式表)][coeff_stream(CoeffCABAC)]
```

- **步长信令（v1.14.1 起）**：type8 载荷自包含亮度/色度步长，解码端反量化
  （`level × q`）完全从载荷读取，**不依赖文件头 lossy_quant**。历史缺陷：
  解码端以 `(lossy_quant, lossy_quant)` 反量化，在 `chroma_scale>1000` /
  显式 `chroma_step` 配置下色度步长错误；且无损 type8 帧出现在有损文件时
  亮度步长错误。步长按平面使用：Y 用 `luma_step`，Co/Cg 用 `chroma_step`。
- 模式表走 RLE+CABAC（v3 载荷 `[k][body]`，`k` 在子载荷首字节）；
  系数流走 CoeffCABAC（4 上下文：ctx_nonzero / ctx_run / ctx_level_q / sign 直通），
  从 last_nz 逆序的 `(run, level, sign)` 三元组，位置信息嵌入 run（无单独位置流）。
- DC 模式（transform skip）直通量化残差；H/V/MED 模式走 DCT8 + 量化 + zigzag。

### 3.7 预测模式（PredictionMode）

| 值 | 名称 | 公式 | 来源参考 |
| :-- | :-- | :-- | :-- |
| 0 | None | 无预测 | — |
| 1 | Horizontal | left | H.264 Intra 10L |
| 2 | Vertical | top | H.264 Intra 10T |
| 3 | Average | (left+top)/2 | H.264 DC 简化 |
| 4 | DC | (left+top+top_left+top_right)/4 | H.264 DC |
| 5 | Med | JPEG-LS LOCO-I 中值边缘检测 | JPEG-LS |
| 6 | Paeth | 三邻居梯度比较选最近者 | PNG filter 4 / AV1 |
| 7 | TopRight | 上行右移一位（右上邻居）；最右列回退 top，首行回退 left | AV1 D45 因果简化版（v1.9） |
| 8 | Diagonal | 沿主对角线上溯 min(x,y) 步至边界像素；首行/首列回退 left/top | AV1 D135 因果简化版（v1.9） |
| 9 | Vertical2 | 上方第 2 行同列像素；y==1 回退 top，y==0 回退 left，原点回退 0 | 多参考行双窗（v1.13） |
| 10 | Horizontal2 | 左侧第 2 列同行像素；x==1 回退 left，x==0 回退 top，原点回退 0 | 多参考列双窗（v1.13） |

所有模式仅依赖因果邻居（左/上/左上/右上/左 2/上 2），保证编解码两端重建一致。边界外邻居取 0。
斜向模式（v1.9 新增）针对二次元插画高频出现的 ±45° 线条走向（头发丝、裙褶、
网格），在精确斜线图案上内部点残差恒为零。
多参考行/列模式（v1.13 新增）将参考窗扩至双行/双列——2 像素周期网点/
细条纹在单行参考下产生满幅方波残差，V2/H2 使其内部点残差恒为零；
候选经同一 SAD 排序竞争管线自动取舍（单调不劣化），SAD 决策成本 8→10 模式。

### 3.8 YCoCg-R 可逆色彩变换（has_rct=1 时）

参考 AV1 / HEVC RExt / JPEG-XL 无损模式的标准做法，对 3 分量格式在空间预测前应用：

```
正变换:                     逆变换:
Co = R - B                  t  = Y - (Cg >> 1)
t  = B + (Co >> 1)          G  = Cg + t
Cg = G - t                  B  = t - (Co >> 1)
Y  = t + (Cg >> 1)          R  = Co + B
```

纯整数运算，`>>` 为算术右移（floor 移位，对负值同样正确），数学严格可逆。变换后像素布局仍为逐像素交错 `[Y, Co, Cg]`。

### 3.9 RLE+Golomb 位流格式（frame_type=1/2/3 内部）

按顺序扫描残差序列：

| 符号 | 编码 | 适用 |
| :-- | :-- | :-- |
| 零行程 | escape `1` + exp-Golomb(行程长度) | 连续零段（长短行程均紧凑：行程 3072 仅 25 bit） |
| 非零值 | escape `0` + Golomb-Rice(k)(zigzag(v)) | 孤立非零样本 |

k 值按数据绝对均值自适应（帧级或条带级独立计算）。

### 3.10 文件尾（Footer，8 字节）

| 长度 | 字段 | 说明 |
| :-- | :-- | :-- |
| 4 | `crc32` | 除文件尾外全部文件内容的 CRC32（LE） |
| 4 | `magic` | 固定 `"CRF\xFF"`（0x43 0x52 0x46 0xFF） |

---

## 4. 编码管线

```
输入帧序列 [原始首帧 + 差分帧...]
   │
   ▼
① YCoCg-R 可逆色彩变换            （3 分量格式，flags.bit1 置位；
   │                               首帧额外参与 bit3 直通竞争，见下）
   ▼
② 逐帧决策（每帧独立执行，取字节最小者）：
   │   ├─ 帧级：6 种预测模式 SAD 排序 → top-2 试编码取最小
   │   │        └─ 首帧额外做 块级k vs RLE+Golomb 双路熵编码竞争
   │   ├─ 条带级：32 行条带各自 SAD 排序 → top-2 试编码（frame_type=2）
   │   └─ 三平面：Y/Co/Cg 各自走完整管线后打包（frame_type=3）
   │
   ▼
③ 组装：文件头 + 帧索引 + 帧数据 + CRC32 尾

首帧 RCT 双路竞争（v1.13）：首帧以「RCT 域」与「RGB 直通」两个色彩域
各走一遍完整自适应管线（两路 Fast-Fail 上限独立计算），字节最小者胜出；
直通胜出时置位文件头 flags.bit3，解码端出口跳过 frame0 的逆变换。
高饱和纯色/换装插画场景 RGB 域更稀疏（G 通道恒零 → 零行程极长，
而 YCoCg-R 后三通道皆有值），实测可获显著收益；自然相关内容 RCT 恒胜出，
体积与旧版逐字节一致（单调不劣化由竞争结构保证）。
```

解码为对称逆过程，全部参数从码流自描述字段获得。

---

## 5. 性能基准

### 5.1 无损模式（PNG1000：14 张 1024×1820 RGB 插画差分，PNG 合计 16.64 MB）

| 方案 | 大小 | 相对 PNG |
| :--- | ---: | ---: |
| 无预测 | 40.80 MB | +145% |
| 水平预测 | 16.21 MB | −2.6% |
| 垂直预测 | 14.86 MB | −10.7% |
| 平均预测 | 14.28 MB | −14.2% |
| DC 预测 | 14.99 MB | −9.9% |
| MED 预测（JPEG-LS） | 14.34 MB | −13.8% |
| **逐帧+条带+三平面+CABAC 全自适应** | **10.35 MB** | **−37.8%** |

全部方案往返校验像素级一致；自适应路径 ≈260ms/帧（rayon 多核并行）。
（v1.9 斜向预测 + 条带高度自适应 + MA 三属性后由 10.56 MB 进一步降至 10.35 MB；
v1.10 有损路径新增 8×8 变换 + Trellis，q90 由 5.62 降至 4.11 MB、q75 至 2.08 MB）

### 5.2 真有损 vs 无损：四图像组对比（v1.4）

有损质量档位 → 量化步长：q90→Q=2，q75→Q=5，q50→Q=10。
体积单位 MB；"基准"为输入文件合计大小。
四组**均为同图差分序列**（服装/表情/场景差分），区别仅在源容器格式。

| 图像组 | 规格 | 源基准 | 无损自适应 | 有损 q90 | 有损 q75 |
| :--- | :--- | ---: | ---: | ---: | ---: |
| PNG1000 | 14×1024×1820 插画差分 | 16.64 (png) | **10.63** (−36%) | 4.36 | **1.47** |
| PNG2000 | 8×3541×2508 jpg 差分 | 41.18 (jpg) | **15.32** (−63%) | 16.23 | 16.01 |
| c 组 | 7×3840×2160 jpg 差分 | 43.84 (jpg) | **57.00** (+30%) | — | — |
| d 组 | 8×3840×2160 jpg 差分 | 74.51 (jpg) | **118.20** (+59%) | — | — |
：有损列为 v1.6 启用色度半分辨率后的 PNG1000 实测值；其余组的最新完整数据见测试输出。

要点：
- **PNG 无损源组**（1000/2000）：像素干净、帧间相关性强，
  无损已显著优于源容器；
- **JPEG 有损源组**（c/d）：同为同图差分，但各帧自带 JPEG 压缩噪声——
  未变化区域的像素在相邻两帧的 JPEG 解码结果中也存在量化差异，
  这些噪声进入差分残差，使无损残差能量远高于 PNG 组。
  无损 CRF 保存噪声是"逐位一致"义务所致（+38%/+43%）；
- **量化即滤噪**：真有损的死区量化恰好将 JPEG 噪声归零——
  c 组 q50 仅 14.65 MB（比源 JPEG 还小 67%），且 PSNR≥34dB 的失真中
  大部分正是被滤除的源噪声本身，观感损失远小于数值提示；
- **色度半分辨率对标 yuv420p**：AVIF 基准（av1_nvenc crf30 yuv420p，3.21 MB / PSNR 37.23dB）
  vs CRF q50+half-res（2.38 MB / PSNR≈37.7dB）——体积再省 26%，质量持平；
- **质量档位收敛为 q90/q75 两档**：q50 及以下在无损源（PNG）上产生可见噪点，已移除；
- 有损源（jpg）组 q75 的量化噪声被源 JPEG 噪声掩蔽，观感无差异；
- 近无损档 q90 存在"量化收益 < 熵编码劣化"的交叉点（约 q<88 收益转正），
  选择建议：追求绝对保真用无损，可接受失真直接用 q50–q75；
- 有损模式下条带/调色板候选自动跳过（闭环量化使多候选收益归零），
  planar 与 CABAC 候选照常参与。

### 5.3 有损误差特性（v1.6 首帧参考）

残差基准采用**首帧参考（golden reference）**而非前帧链式累加：

```
residual[i] = frame[i] − frame[0]      （RGB 域差分，再经 RCT）
restored[i] = restored[0] + dequant(residual[i]) = frame[i] + e_i
```

- **误差零累积**：每帧重建误差仅为该帧自身量化误差 e_i ≤ ⌊Q/2⌋，
  与帧序无关——实测 PNG1000 q50 全帧 PSNR 稳定在 44~49dB（无衰减）；
- **残差纯净**：不混入其他帧的量化噪声，平坦区域噪点显著减少；
- **帧间零依赖**：全部帧可并行编码、任意帧独立随机访问解码、
  单帧损坏不扩散；
- 差分在 RGB 域进行后再 RCT（与解码端 rct_inverse 出口同域），
  规避 YCoCg-R 移位舍入的非线性差异；
- keyframe_interval 语义更新为「无损锚点间隔」：锚点帧以 Q=1
  高精度重存差分，用于长序列的精度重校准。

---

### 6.1.1 q95 视觉无损档（v1.12，对标 AVIF cq18 / HEIF crf30）

lossy_quality: Some(95) 定位为**视觉无损**：全部帧 max_err ≤ 1、
实测 PSNR 54~102dB（远高于 AVIF cq18 的典型 45~50dB）。

机制组成：
1. **空间域 ±1 轻滤**：差分帧 |v|≤1 归零（±1 为插画差分最高频小幅值，
   归零后 RLE 零行程显著增长；±1 灰度差低于人眼 JND）；
2. **DCT 矩阵高频轻滤**：Q=1 基础 + 感知矩阵缩放许可（低频 Q_pos=1
   完全保留，最高频 Q_pos=2）；
3. **golden_lossless 开关**（LossyTuning，默认 true）：超小序列（2 帧）
   首帧占体积 53~97%，置 false 时首帧同样轻滤——q95 兑现收益的关键开关。

实测（golden_lossless=false）：组1 −0.9%、组2 −0.3%、组3 −11.8%、组5 −15.5%
（相对无损自适应）；JPG 噪声源收益最大。

## 6.1 可调参数（真有损精细控制）

`EncodeParams.lossy_tuning: Option<LossyTuning>`，None 时全部取默认值：

| 参数 | 默认 | 说明 | 参考 |
| :-- | :-- | :-- | :-- |
| `chroma_quant_percent` | 130 | 色度平面（Co/Cg）量化步长按此百分比放大。人眼对色度失真不敏感，>100 可在不明显降低观感下进一步缩小体积 | JPEG 4:2:0 / HEVC chroma_qp_offset |
| `keyframe_interval` | 10 | 每 N 帧插入一个无损刷新帧，阻断差分链量化误差累积；0=仅首帧无损 | H.264/AV1 IDR interval |
| `deadzone_bias` | 0 | 死区偏置（-32..32，/64 定点）：正值更激进归零小残差（更小体积、观感略降），负值保细节 | x264 deadzone |
| `chroma_half_res` | true | Co/Cg 平面 2×2 下采样（对标 yuv420p） | AVIF/HEVC |
| `anchor_quality_percent` | 100 | 锚点帧（间隔边界）步长折算百分比；golden 首帧始终强制无损 | — |
| `noise_adaptive` | false | **JPEG 源噪声感知**（v1.8）：差分帧按条带估计失真水平，启用两级机制——①零中心性门控软阈值；②闭环 per-band 自适应步长。详见下方说明 | — |
| `noise_tau_x100` | 150 | 噪声阈值系数 τ×100：软阈值 T=τ·amp25、band 步长 Q_eff=τ·amp25。越大滤噪/压缩越激进 | — |

**噪声归一化两级机制**（`noise_adaptive=true` 且有损模式时生效）：

1. **零中心性门控软阈值**：统计差分场的带符号中位数 `med` 与幅度 P25 分位
   `amp25`；仅当 |med| ≤ max(amp25, 2)（场围绕 0 对称）时，将 |v| ≤ τ·amp25
   的残差归零。系统性偏移场（时间差分的色调漂移）与两极化结构场
   （服装/表情差分）被门控自动拦截，零介入。
2. **闭环 per-band 自适应步长**：每 BAND_HEIGHT(32) 行条带按三分量合并的
   |v| P25 推导有效量化步长 Q_eff = clamp(τ·amp25, base_step, 16)，注入闭环
   量化器逐行查表——失真稠密的条带（天气颗粒/色调漂移）死区自动加宽，
   静止为主的条带保持基础步长精细度。空间预测天然完成"中心化"
   （常数漂移被邻居预测抵消），故无需零中心性判定。

正确性保证：解码端无感（各条带 Q_eff 倍数的自描述残差），格式与码流结构零改动；
无损模式不受任何影响。

## 6.2 并行与加速

- **帧间并行**：残差帧均基于原始首帧差分、相互独立，rayon 按帧并行编码；
- **条带并行**：frame_type=2 的条带间无依赖，按条带并行；
- **决策加速**：预测模式排序采用行采样 SAD（1/4 开销）+ 条带残差缓存；
- 大分辨率场景可通过 `RAYON_NUM_THREADS` 控制线程数以限制内存峰值。

---

## 6.3 算法优化实施记录（v1.5）

| 优化项 | 来源建议 | 实现方式 | 效果 |
| :-- | :-- | :-- | :-- |
| 多 k 值精确竞争 | §2.1 | 非零 zigzag 直方图单遍统计，对 k∈0..6 精确计算 Golomb 总位长取最小（O(N)，非暴力重编） | 无损自适应 −7%；无预测档 −43%（旧均值法 k 偏离最优） |
| 帧间模式继承 | §1/§8 | 首帧串行编码提取胜出 pred_mode，后续帧以其为优先候选；planar 子平面链式继承（Y→Co→Cg） | 决策收敛加速，SAD 排序波动免疫 |
| CRC32 硬件加速 | §7 | crc32fast 替换查表实现（多项式/初值一致，产物位兼容） | 校验提速数倍 |
| CfL α=0 候选修复 | 附带发现 | α=0（关闭 CfL）重新加入竞争——平坦色度平面不再被强制非零 α 注入亮度纹理 | planar 单调性恢复 |
| 有损源滤噪提示 | §7 | 检测 jpg/jpeg/webp 源时输出量化滤噪建议 | 引导有损档位选择 |

**暂缓项**（评估后列入后续路线）：SIMD 向量化（std::simd 未稳定）、CABAC 上下文扩展至梯度分级、自适应变换块大小、RDO 率失真决策、符号分离编码。

## 6.3.1 算法优化实施记录（v1.9）

外部系统性优化建议的完整评判（含暂缓理由）见 `docs/optimization-review.md`；采纳实施的四项：

| 优化项 | 来源建议 | 实现方式 | 效果 |
| :-- | :-- | :-- | :-- |
| 斜向预测模式 | §2.1 前半 | 新增 TopRight(45°)/Diagonal(135°)（AV1 D45/D135 因果简化版，pred_mode=7/8），候选进入既有 SAD 排序竞争管线，单调不劣化 | PNG1000 无损自适应参与贡献 −2.0%（10.56→10.35 MB）；斜线图案内部点残差恒零 |
| MA 树第三因果属性 | §1.1 | 属性集 +\|top_left\|（2→3 维），训练采样与编解码两端 attrs 推导对称扩展；树序列化格式不变 | 对角边缘走向可分性提升，CABAC 变体竞争力增强 |
| DCT 感知量化矩阵 | §3.2 | frame_type=6 增加 flat/matrix 双版完整试编码竞争；4×4 权重表 /64 定点（DC×1.0→高频×2.25），载荷首字节 bit6 标注；Q=1 近无损档强制禁用加粗；解码端零改动 | 有损 q75/q90 高频步长按 CSF 加粗，同体积下主观质量提升 |
| 条带高度自适应 | §5.1 | frame_type=2 条带高度参数化：32 行恒参与、帧高 ≥128 时追加 64 行档竞争，帧头 coding_params 自描述实际值（解码端校验 {32,64}） | 平坦插画摊薄条带头开销、增长共享行程 |

**v1.9 暂缓项**（理由详见评审文档）：IntraBC 块复制、CfL 非线性映射、预测融合、符号分离编码、8×8/16×16 多尺寸变换核、RDOQ/Trellis、视觉掩蔽 τ 联动、分层 B 帧、帧头压缩、调色板跨帧复用。

---

## 6.4 版本历史

| 版本 | 主要变更 |
| :-- | :-- |
| 1.0 | 初版：Golomb-Rice / 指数哥伦布 / Hadamard 变换 |
| 1.1 | RLE+Golomb 混合编码；MED/DC 预测；逐帧自适应模式选择；帧头扩展 pred_mode 字段（10→11 字节） |
| 1.2 | 零行程改用 exp-Golomb；首帧双路熵编码竞争；条带自适应（frame_type=2）；三平面打包（frame_type=3）；YCoCg-R 可逆色彩变换（flags.bit1）；PAETH 预测；Hadamard 整数精确可逆化修复 |
| 1.3 | 调色板模式（frame_type=4）；RLE+CABAC 自适应算术编码（frame_type=5，9 上下文 + 分桶直通行程二值化）；CfL 色度亮度线性预测（planar 内 α 搜索）；变换核替换为 lifting 可逆整数 DCT；rayon 帧级/条带级并行与决策加速 |
| 1.4 | **真有损压缩**：残差闭环死区标量量化（flags.bit2 + 文件头 byte21 存 Q），与无损路径并行可选；JPEG/WebP 输入支持；编码器/解码器 mod.rs 模块化拆分（单文件 ≤1000 行标准） |
| 1.5 | **算法精修**：RLE 多 k 直方图精确竞争；帧间预测模式继承（首帧引导 + planar 子平面链式）；CfL α=0 候选修复；CRC32 换用 crc32fast 硬件加速路径；有损源容器滤噪提示 |
| 1.6 | **色度半分辨率 + 首帧参考重构 + 质量档位精简**：Co/Cg 平面 2×2 下采样对标 yuv420p；残差基准改为首帧 golden（误差零累积、全并行、随机访问解码）；移除 q50 档，保留 q90/q75 有损预设；四图像组全方案实测 40/40 PASS |
| 1.7 | **有损管线 DCT 变换域量化**：新增 frame_type=6 候选（4×4 lifting DCT → 死区量化 → RLE+CABAC）；锚点帧统一量化（anchor_quality_percent=100 对标 AVIF 全帧 CRF）；LossyTuning 扩展 chroma_half_res/anchor_quality_percent；zigzag 4×4 表修复；测试数据修正为高相关帧序列 |
| 1.8 | **JPEG 源噪声感知（noise_adaptive）**：差分残差按条带估计幅度 P25，两级机制——①零中心性门控软阈值（带符号中位数偏离 ≤ amp25 才启用，系统性偏移/结构场零介入）；②闭环 per-band 自适应步长（死区宽度跟随局部失真水平，CAP=16）。**CABAC 上下文分类器三模式竞争（v3）**：MA 树数据驱动决策树（JPEG-XL Meta-Adaptive 同款，因果属性 |left|/|top| 贪心熵减训练、树头序列化进载荷）/ 固定梯度 4 档 / 全帧统一，帧内取码流最小者保证单调不劣化。**调色板 v2**：copy-above token 化索引流（AV1 palette 思路适配），coding_params.bit0 标记载荷版本。修复 v1.7 DCT 路径三重缺陷；golden 首帧强制无损。纯场景差分-1 实测 q90 −28.5%（噪声感知+MA）|
| — | **SIMD 向量化 + 流式编码 API**：`format/simd.rs` AVX2 运行时分派（is_x86_feature_detected）覆盖差分计算/YCoCg-R 变换/软阈值（补码绝对值修正，与标量逐位一致）；CRC32 已由 crc32fast PCLMULQDQ 硬件加速。新增 `encoder/streaming.rs` 流式编码器：逐帧推送、内存 O(golden+单帧+码流)、解除 50 帧上限（至 65535 帧）、输出与批量编码逐字节一致；MAX_FRAMES 常量放宽 |
| 1.9 | **算法优化四连**（外部建议评判后采纳，全记录见 docs/optimization-review.md）：①斜向预测 TopRight(45°)/Diagonal(135°)（AV1 D45/D135 因果简化版，pred_mode 7/8）；②MA 树第三因果属性 \|top_left\|（对角边缘可分性）；③DCT 感知量化矩阵（frame_type=6 flat/matrix 双版竞争，载荷 k 字节 bit6 标注，Q=1 恒等保护）；④条带高度自适应（frame_type=2 的 {32,64} 竞争，coding_params 自描述）。PNG1000 无损自适应 10.56→10.35 MB（−2.0%），9/9 方案往返校验 PASS |
| 1.10 | **多尺寸变换核 + RDOQ**（循环轮 R1/R2）：①8 点 lifting DCT（transform/dct8：镜像蝶形对称/反对称分离骨架，探针验证频率槽位单调）；②frame_type=6 扩展 {4×4,8×8}×{flat,矩阵} 四版竞争（bit5=8×8 标记）+ Trellis 胜者再竞争（rdoq.rs 两状态 Viterbi DP、λ=8.5Q²、bit4 标记）。q90 有损 5.62→**4.11 MB（−26.9%）**、q75 至 2.08 MB；无损路径零影响。工程分类：transform/{dct4,dct8} 与 dct_path/{mod,qm} 目录化，全部文件 ≤1000 行 |
| 1.11 | **帧内块复制 IntraBC**（循环轮 R3/R4）：新 frame_type=7——8×8 块级 COPY/PRED 决策，双粒度搜索（块对齐签名桶 + 行段辅助索引：深度 32 行、步长 2 像素，支持非块对齐重复源）；三段式载荷（决策 RLE / COPY 向量 exp-Golomb / PRED 预测残差 RLE）。无损路径全帧参与（含差分帧）。开发中修复三处对称性缺陷：①DC/TopRight 的 top_right 跨块引用在块级光栅序下破坏因果性（PRED 候选剔除）；②RLE 零行程跨块截断丢失（残差流一次性整体解码后按块切片）；③COPY 因果条件不完备（改为 syu+BS≤by ∥ sxu+BS≤bx∧syu≤by 完备形式）。100/101→101/101 测试 PASS；PNG1000/2000/c 端到端 27/27 方案 PASS，体积持平（单调不劣化由竞争兜底） |
| 1.12 | **矩形变换核**（循环轮 R9）：transform/rect.rs——4×8/8×4 矩形 DCT（行列分离复用 dct4/dct8 一维 lifting 核，可逆性由构造保证）+ WIDE/TALL 感知权重表（32 元素）；frame_type=6 有损竞争扩至六版（载荷 k 字节 bit5=宽8、bit7=高8 组合形状码，旧文件 bit7 恒 0 向后兼容）；dct_path 全链路与 rdoq 参数化 (bw,bh)。三组端到端体积持平（真实插画方形已最优，矩形能力保留待方向性纹理自动兑现），106/106 测试 PASS |
| — | **R5/R7 收尾**：SAD 决策 rayon 并行化；MA 树第四属性 \|top_right\|（4 维因果属性）；**无损路径放开 DCT(Q=1) 候选**——lifting DCT 严格可逆、Q=1 量化恒等保证逐位无损，frame_type=6 无损新增 {4×4,8×8} flat 两版竞争（探针实测斜线/纹理内容较 Med+RLE 小 16%~31%；真实插画差分未胜出由竞争兜底，纹理型内容自动受益）。噪声感知 A/B 实验证伪自动启用价值（2000/c 组零介入：噪声 P25 低于步长门限，保护性设计正确）。λ 多档竞争经论证跳过（与质量档位语义冲突） |
| 1.13 | **RCT 首帧自适应**（首帧专项 #3）：文件头 flags 新增 bit3 `first_frame_no_rct`——编码端对 golden 首帧做「YCoCg-R 域 vs RGB 直通」双路完整管线竞争（字节最小者胜出、平局保守保持 RCT），直通胜出置位 bit3；解码端 decode_from_bytes/decode_from_file 出口按标志跳过 frame0 的 rct_inverse。批量路径 G/C 与 streaming 三处对称实施（流式与批量输出逐字节一致由测试锁定）；差分帧不受影响（差分基准是首帧像素值而非存储格式）。高饱和纯色极端场景下调色板候选在两域收敛、平局保守；G=0 宽纹理场景（真实换装插画的典型形态）直通显著胜出。顺带修复流式路径 Gray 序列误走 rct_forward 报错的缺陷。111/111 测试 PASS；PNG1000 端到端 10/10 方案 PASS、体积与基线逐字节持平 |
| — | **R13 循环轮三连**：①**无损 DCT 矩形补齐**（首帧专项 #5/R9 延伸）——frame_type=6 无损 variants 从 {4×4,8×8} flat 两版扩至四版（+{8×4,4×8}，Q=1 恒等语义下形状仍有效），载荷 bit5/bit7 形状码与有损共用、解码端零改动；②**超块分区先导验证**（首帧专项 #1）——新增 `--probe-split` 探针（banded.rs 条带列方向二分收益测量，predict_at 全帧坐标取邻居保证与实施语义一致），PNG1000 全组实测 32 行档 banded 载荷仅省 **0.46%**、64 行档 **0.25%**——折算全帧影响 <0.3%，远低于工程阈值，**完整四叉树数据驱动关闭**；③**多参考行/列预测 V2/H2**（首帧专项 #2）——PredictionMode 扩 9/10（上 2 行/左 2 列位移参考，2px 周期网点/细条纹内部点残差恒为零），ADAPTIVE_CANDIDATES 8→10 经同一 SAD 竞争管线自动取舍，IntraBC 白名单维持子集不动。113/113 测试 PASS；PNG1000 端到端 10/10 PASS 体积与基线持平（能力保留待纹理型内容自动兑现） |
| — | **R14 熵编码批量位写入**（R8 遗留 S6 补齐）：三个逐位编码器（RLE+Golomb / exp-Golomb / Golomb-Rice）各增 `write_bits_msb(n≤57)` 批量原语——Golomb 商分批+尾批合并单次写入、exp 前导零天然高位对齐单次写入、RLE 零行程 escape 合并（总位 2m+2）；开发中经回归捕获并修复两处自引入缺陷（escape 合并位宽误算、分批场景前缀错位），新增逐位参考实现五类数据集对拍锚点。114/114 测试 PASS；PNG1000 端到端体积与基线**逐字节一致**（码流零改动最终证明），端到端耗时 ≈−3%（被 I/O 与 CABAC 主力路径稀释） |

---

## 7. 后续演进方向

---



- **JPEG 源噪声感知**：检测源容器为有损格式时，自动建议/启用滤噪档位
  （量化即滤噪，c 组 q50 实证 −67%）；进一步可研究 DCT 域直接转码
  （JPEG→CRF 免解码，保留频域信息避免双重变换）
- 上下文算术编码深化：按局部梯度分级扩展上下文集合（当前 9 个 → 数十个）
- 调色板与预测混合编码（AV1 palette + delta 思路，适配超低色数插画）
- MA 树上下文建模（JPEG-XL 同款）
- SIMD 向量化残差计算与 CRC32
- 流式编码 API 支持 >50 帧或超大分辨率序列
- 扩展属性集（如 EP 预测误差）
